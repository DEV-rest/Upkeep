export type ServiceCategory = {
  title: string;
  description: string;
  detail: string;
};

export type ExampleProject = {
  title: string;
  label: string;
  image: string;
};

export type Testimonial = {
  name: string;
  location: string;
  quote: string;
  image: string;
};
